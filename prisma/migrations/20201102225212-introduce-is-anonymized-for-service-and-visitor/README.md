# Migration `20201102225212-introduce-is-anonymized-for-service-and-visitor`

This migration has been generated by Kai Wittmann at 11/2/2020, 10:52:12 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `kkm2`.`Service` ADD COLUMN `isAnonymized` boolean NOT NULL DEFAULT false ;

ALTER TABLE `kkm2`.`Visitor` ADD COLUMN `isAnonymized` boolean NOT NULL DEFAULT false ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200710021113-add-dynamic-field..20201102225212-introduce-is-anonymized-for-service-and-visitor
--- datamodel.dml
+++ datamodel.dml
@@ -2,50 +2,52 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url      = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
 }
 model Service {
-  id                      Int       @default(autoincrement()) @id
+  id                      Int       @id @default(autoincrement())
   serviceStartsAt         DateTime
   registrationStartsAt    DateTime?
   registrationEndsAt      DateTime?
   numberOfAllowedVisitors Int
   additionalInfo          String?
   noDateConflict          Boolean   @default(false)
   visitors                Visitor[]
+  isAnonymized            Boolean   @default(false)
   createdAt               DateTime  @default(now())
 }
 model Visitor {
-  id             Int      @default(autoincrement()) @id
+  id             Int      @id @default(autoincrement())
   name           String
   surname        String   @default("")
   street         String
   zip            String
   city           String
   phone          String
   email          String?
   additionalData Json?
+  isAnonymized   Boolean  @default(false)
   service        Service  @relation(fields: [serviceId], references: [id])
   serviceId      Int
   createdAt      DateTime @default(now())
 }
 model Text {
-  id    Int     @default(autoincrement()) @id
+  id    Int     @id @default(autoincrement())
   key   String
   value String?
 }
 model DynamicField {
-  id       Int     @default(autoincrement()) @id
+  id       Int     @id @default(autoincrement())
   key      String
   label    String
   type     String
   required Boolean @default(false)
```


